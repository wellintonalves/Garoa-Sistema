# URL do backend no Railway — cole aqui depois do deploy
VITE_API_URL="http://localhost:3001"

# Nome da barbearia (aparece na tela de login e menu)
VITE_BARBEARIA_NOME="Garoa Barbearia"

# Atenção para as variáveis do backend:
# O backend agora exige variáveis separadas sem fallback para segurança.
# Certifique-se de configurar no seu host (ex: Railway):
# JWT_SECRET="gere-um-valor-aleatorio-com-pelo-menos-24-caracteres"
# JWT_SECRET_CLIENTE="gere-um-valor-aleatorio-com-pelo-menos-24-caracteres"
# JWT_SECRET_BARBEIRO="gere-um-valor-aleatorio-com-pelo-menos-24-caracteres"
