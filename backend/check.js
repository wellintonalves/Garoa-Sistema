require('dotenv').config({ path: '.env' }); const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function run() { const bs = await prisma.barbeiro.findMany({ include: { agendamentos: { select: { id: true }, take: 1 }, comissoes: { select: { id: true }, take: 1 }, movimentacoes: { select: { id: true }, take: 1 }, usuario: { select: { nome: true } } } }); let safe = [], hist = []; bs.forEach(b => { if (b.agendamentos.length || b.comissoes.length || b.movimentacoes.length) hist.push(b.usuario.nome); else safe.push(b.usuario.nome); }); console.log('Seguros:', safe.length, safe); console.log('Com Historico:', hist.length, hist); } run();  
